/// Valores de `profiles.role` alinhados à modelagem (funcionário, atendente, administrador).
abstract class UserRoles {
  static const admin = 'admin';
  static const employee = 'employee';
  static const attendant = 'attendant';
}
